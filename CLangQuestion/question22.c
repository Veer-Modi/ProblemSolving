// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main() {
    
    char str[]="hello";
    int len=strlen(str);
    printf("%d",len);

    return 0;
}
